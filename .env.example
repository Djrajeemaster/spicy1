# Environment Configuration for API Endpoints
# Copy this file to .env.local for development or .env.production for production

# ===== LOCAL DEVELOPMENT =====
# For local development (when running on localhost:3000)
# Set this when running the app in development mode
API_BASE_URL=http://localhost:3000

# React Native Expo public environment variable
# This will be available in the client-side code
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# Domain URL for sharing and external links (local development)
DOMAIN_URL=http://localhost:3000

# ===== VPS/PRODUCTION =====
# For VPS/Production deployment, comment out the localhost URLs above
# and uncomment the lines below, replacing with your actual domain

# For production, use relative URLs (no domain needed)
# API_BASE_URL=

# Your production domain for sharing and external links
# DOMAIN_URL=https://yourdomain.com

# If you need to specify a different domain for API calls in production:
# API_BASE_URL=https://yourdomain.com

# React Native Expo public environment variable for production
# EXPO_PUBLIC_API_BASE_URL=https://yourdomain.com

# ===== OTHER ENVIRONMENT VARIABLES =====
NODE_ENV=development
PORT=3000

# Database connection (adjust according to your setup)
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# ===== SUPABASE CONFIGURATION (if using) =====
# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Example:
# EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here